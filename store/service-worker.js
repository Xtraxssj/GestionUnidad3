// Código del service worker

self.addEventListener('install', e => {
    // Caches y otros recursos
  });
  
  self.addEventListener('fetch', e => {
    // Manejo de peticiones HTTP
  });
  